/**
 * Archivo que crea el contexto, creando un objeto mediante la funcion createContext
 */

import { createContext } from "react";

const ProductContext = createContext();

export default ProductContext;
